codesy.mirzabekov.space {
  log / /etc/logs/requests.log "{combined}"
  proxy / web:3000
}

api.codesy.mirzabekov.space {
  log / /etc/logs/requests.log "{combined}"
  proxy / server:4000
}

goaccess.codesy.mirzabekov.space {
  log / /etc/logs/requests.log "{combined}"
  root /site/goaccess
  gzip
}

goaccess.codesy.mirzabekov.space:7890 {
 proxy / goaccess:7890 {
   websocket
 }
}